[phases.install]
cmds = [
    "pip install -r Django/requirements.txt"
]

[phases.build]
cmds = [
    "python ML/scripts/train_model_realistic.py"
]

[start]
cmd = "cd Django && python manage.py migrate --noinput && gunicorn health_assistant.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"